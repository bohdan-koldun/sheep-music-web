import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TagList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Песни по тематикам',
  },
});
