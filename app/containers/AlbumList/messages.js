/*
 * AlbumList Messages
 *
 * This contains all the text for the AlbumList container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AlbumList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AlbumList container!',
  },
});
