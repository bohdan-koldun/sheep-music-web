import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddAlbum';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddAlbum container!',
  },
});
