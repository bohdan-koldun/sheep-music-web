import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddAuthor';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddAuthor container!',
  },
});
