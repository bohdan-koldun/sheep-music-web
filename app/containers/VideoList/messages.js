import { defineMessages } from 'react-intl';

export const scope = 'app.containers.VideoList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Найти видеоклип',
  },
});
