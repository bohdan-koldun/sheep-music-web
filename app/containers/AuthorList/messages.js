/*
 * AuthorList Messages
 *
 * This contains all the text for the AuthorList container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AuthorList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AuthorList container!',
  },
});
