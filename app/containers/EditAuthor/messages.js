import { defineMessages } from 'react-intl';

export const scope = 'app.containers.EditAuthor';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EditAuthor container!',
  },
});
