import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddSong';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddSong container!',
  },
});
